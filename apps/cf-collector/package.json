{
	"name": "cf-collector",
	"version": "1.0.0",
	"description": "Cloudflare Worker to intercept and forward page_visit events",
	"scripts": {
		"dev": "wrangler dev",
		"wrangler:login": "wrangler login",
		"wrangler:deploy": "wrangler deploy",
		"gen:types": "wrangler types",
		"deploy:production": "wrangler deploy --env production",
		"tail": "wrangler tail",
		"script:test": "./test-local.sh"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20240806.0",
		"typescript": "^6.0.3",
		"wrangler": "^4.17.0"
	}
}
